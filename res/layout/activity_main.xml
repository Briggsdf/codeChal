<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:visibility="visible"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/buttonView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="16dp"
        android:onClick="viewFolder"
        android:text="ViewFolder" />

    <Button
        android:id="@+id/buttonUpload"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/buttonView"
        android:layout_alignBottom="@+id/buttonView"
        android:layout_alignParentRight="true"
        android:layout_marginRight="14dp"
        android:onClick="upload"
        android:text="Upload" />

    <Button
        android:id="@+id/buttonCapture"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/buttonView"
        android:layout_alignTop="@+id/buttonAuthiD"
        android:onClick="takePicture"
        android:text="@string/buttonCapture" />

    <Button
        android:id="@+id/buttonAuthiD"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="170dp"
        android:onClick="authenticate"
        android:text="@string/buttonAuth"
        android:visibility="visible" />

</RelativeLayout>
